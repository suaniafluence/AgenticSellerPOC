name: 'Dependency Review'

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
    - name: 'Checkout Repository'
      uses: actions/checkout@v4

    # Skip dependency-review-action for now as Dependency Graph needs to be enabled
    # To enable: Go to Repository Settings > Security & analysis > Enable Dependency graph
    # Then uncomment the step below:

    # - name: 'Dependency Review'
    #   uses: actions/dependency-review-action@v4
    #   with:
    #     fail-on-severity: moderate

    - name: 'Placeholder - Dependency Graph Disabled'
      run: |
        echo "⚠️ Dependency Review is currently disabled."
        echo "To enable it:"
        echo "1. Go to: https://github.com/${{ github.repository }}/settings/security_analysis"
        echo "2. Enable 'Dependency graph'"
        echo "3. Then uncomment the dependency-review-action step in this workflow"
        echo "✅ Workflow passed (dependency review skipped)"
